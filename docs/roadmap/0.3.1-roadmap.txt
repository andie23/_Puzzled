- Update modules that depend on Pcache with new changes. New pcache changes break
  existing functionality used by those modules.
- implement functionality to record stats after each finished puzzle
- Automatically add quotation marks on text passed in sqlquery functions. Currently, without adding
  additional quotation marks, the system breaks
